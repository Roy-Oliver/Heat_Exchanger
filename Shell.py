class Shell:
    def __init__(self, m_shell_in, Cp_shell, mu_shell, rho_shell, k_shell):
        self.m_shell_in = m_shell_in
        self.Cp_shell = Cp_shell
        self.mu_shell = mu_shell
        self.rho_shell = rho_shell
        self.k_shell = k_shell